import styled from 'styled-components';

export const StyledDeleteContactContainer = styled.div`
	display: flex;
	gap: 12px;
	max-width: 380px;
	flex-direction: column;
	text-align: center;
	justify-content: center;

	button {
		margin: 10px auto;
	}
`;
